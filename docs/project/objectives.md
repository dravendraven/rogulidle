# Objectives

**This is the root document.** Everything else in the project derives from it;
it derives from nothing. It names no mechanism, no instrument, no file and no
state of the build. The test: if a sentence would stop making sense after the
code was thrown away and rewritten from scratch, it does not belong here.

## The product

The game plays itself. The player chooses, then watches the choice play out,
run after run.

The frame, in the owner's words: **a race you have a bet on.** You want your
horse to win. The race is fun because **reversals are possible** — something
can happen, your favourite can surge or fall back — so there is a rollercoaster
rather than a result. Winning is very rewarding, and it makes you want to watch
the next one.

**The product is a race the player has a stake in.** The stake is small today
and will grow. Nothing below changes when it does.

## What a run has to be

- **Hope must never reach zero.** This is the sharpest form of the goal and it
  subsumes "a reversal is possible". Hope can be *low* — your horse is last —
  or high, but it has to exist, because a run whose outcome is already
  determined is not a race even while it is still moving. And hope does not
  have to be hope of winning: it has to be hope of something **not yet
  decided**. A run carrying one question goes to zero all at once; a run
  carrying several degrades instead.
- **A decided run must end quickly.** The corollary. Hope cannot be restored to
  a state that is already lost, so the only lever left is how long that state
  lasts. The named failure is **the shamble**: a run that keeps going after it
  has been decided.
- **The outcome stays uncertain for as long as possible.** Not balanced —
  *uncertain*. A coin flip at the end beats a foregone conclusion at the start.
- **Winning rewards doing well, and stays rare enough to matter.** A band with
  two named failures: too common and the win loses its charm, too rare and hope
  goes with it. No number is set for the band; the two failure directions are
  what to steer by.
- **The outcome has to be attributable.** It has to be possible to say *why* a
  run went the way it did, from watching it. **Rarity without attribution is a
  lottery rather than a race** — the same win rate, read as noise.
- **It has to make you want the next one.** What makes someone start another
  run is a premise that differs, not an outcome that differs.

## What a choice has to be

The player chooses before a run and the choice pays off during it. That is the
only shape available: nobody steers mid-run, so a decision is made in advance
and then watched to find out whether it was right.

- **Visible.** A choice must produce recurring, recognisable moments. If you
  cannot tell which one was made by watching for thirty seconds, it was not a
  choice.
- **Wrong sometimes.** An option that is simply better is not a choice, it is
  an unlock. Every option needs a real weakness, and one that shows on screen.
- **Attributable.** It has to be possible to look at a win and say the choice
  contributed to it.

## What a loss owes

**Nothing.** A run that is not completed earns nothing, and that stays.

The tempting wrong conclusion is that a lost run must therefore feel worthless.
**What a run grades and what it rewards are separate axes.** A loss can resolve
at different heights, be close or not close, and cost something — none of which
requires paying for it. Where weight is wanted on a loss it comes from the
stake having been chosen, or from what the loss costs. Never from what it pays.

## How work gets found

**Watch it. Fix what is wrong.**

That is the whole method. It is not a fallback from something better — it has
outperformed the alternative by a wide margin.

## A threshold is not a scoreboard

A **scoreboard** is a quantity to push in one direction, with everything
downstream inheriting the assumption that pushing it makes the product better.
That assumption has never once been established here.

A **tripwire** is the opposite shape: it fires or it does not, and when it
fires there is a defect to find. That is what makes "fix what is wrong"
actionable instead of a matter of taste.

Thresholds that detect a defect, yes. Targets to maximise, no. Every property
above is a tripwire — if one starts reading as a quantity to push, it has been
written wrong.

## Measure only when you cannot tell by looking

Most of what is wrong is visible in thirty seconds of watching. Reach for an
instrument when a question is genuinely invisible, and not before.
